spring.profiles.active=${env}
server.port=8009

spring.application.name=microservice-loverent-thirdParty-v1

eureka.instance.appname=${spring.application.name}
eureka.instance.prefer-ip-address=true
#是否启用定时刷新token功能
enable.refreshToken=true